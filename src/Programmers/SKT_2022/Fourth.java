package Programmers.SKT_2022;

public class Fourth {
    // nxm 격자
    // 1x1 -> nxm으로 이동하려고 함
    // 각 칸은 [평지, 숲, 강]
    // 상하좌우로 인접한 '평지' '숲'으로 이동 가능
    // 하루에 최대 k칸만 이동할 수 있음
    // 야영을 한다면 다음 날에 k칸 이동 가능 but 야영은 평지에서만 가능하다. (k칸 다 이동 전에 야영 가능)

    public int solution(String[] grid, int k) {
        int answer = -1;
        return answer;
    }
}
